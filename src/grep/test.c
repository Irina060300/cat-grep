#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <regex.h>

/* Функция для получения подстроки */
static char* substr(const char*str, unsigned start, unsigned end) {
  int j, z;
  printf("%d", (int)'\8');
  return 0;
}//}}int
